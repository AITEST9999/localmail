{
  "name": "@localmail/events",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "types": "./src/index.ts",
    "default": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "eslint src --max-warnings=0",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@localmail/core": "workspace:^",
    "@localmail/db": "workspace:^",
    "bullmq": "^6.3.8",
    "drizzle-orm": "^0.38.3",
    "ioredis": "^5.8.2"
  }
}
