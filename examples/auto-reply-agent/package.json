{
  "name": "@localmail/example-auto-reply-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint src --max-warnings=0",
    "smoke": "tsx src/smoke.ts",
    "test": "pnpm --filter @localmail/sdk build && vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@localmail/sdk": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
